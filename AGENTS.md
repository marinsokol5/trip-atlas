# Trip Atlas maintenance

When changing the itinerary JSON schema, field meaning, validation, or document path rules, update `skills/trip-atlas-update-itinerary/SKILL.md`, the README, and relevant tests in the same change.

Use demos or temporary files for app development and review. Do not modify the parent workspace's personal `itinerary.json` unless the user explicitly requests itinerary editing.
